// Legacy settings.js - now just an alias to constants
// This file exists for backward compatibility
// All actual configuration is now in constants.js

// Re-export constants for existing code compatibility
window.GAME_CONFIG = window.GAME_CONSTANTS;